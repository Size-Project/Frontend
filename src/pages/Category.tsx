import React from 'react';
import ScreenMain from '../app.feature/Main/screen/ScreenMain';
import AppLayout from '../app.layout';

const PageCategory = () => {
  return (
    <AppLayout>
      <ScreenMain />
    </AppLayout>
  );
};

export default PageCategory;
