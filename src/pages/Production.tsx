import React from 'react';
import AppLayout from '../app.layout';
import ProductionLayout from 'app.layout/production';

const Production = () => {
  return (
    <AppLayout>
      <ProductionLayout />
    </AppLayout>
  );
};

export default Production;
