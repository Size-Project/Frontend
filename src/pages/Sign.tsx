import React from "react";
import styled from "styled-components";

const PageSign = () => {
  return <StyledWrapper>회원가입페이지</StyledWrapper>;
};

export default PageSign;

const StyledWrapper = styled.div``;
